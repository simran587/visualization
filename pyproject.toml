[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simple-eda"
version = "0.1.0"
description = "A simple exploratory data analysis project"
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [
    { name = "simran587" },
]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/simple_eda"]
